// Smoke test placeholder — actual integration tests are in integration_test/
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder smoke test', (WidgetTester tester) async {
    // Full app init requires native services (KeyService, SQLite, system tray).
    // Run `flutter test integration_test/` for full E2E coverage.
    expect(true, isTrue);
  });
}
